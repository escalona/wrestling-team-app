<div class="new-page">
  <h1 class="new-header">Add Match for <%= @wrestler.first_name.capitalize %> <%= @wrestler.last_name.capitalize %></h1>

  <%= render 'form' %>
</div>

<%= link_to 'Back', wrestler_path(@wrestler), class: 'btn' %>