<div class="new-page">

  <h1 class="new-header">New Wrestler</h1>

  <% if @wrestler.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wrestler.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @wrestler.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= render 'form' %>
</div>

<%= link_to wrestlers_path, class: 'btn' do %>
  <i class="icon-home "></i> Back
<% end %>